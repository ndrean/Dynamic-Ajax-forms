console.log("respond to move by assing form after update resto.genre") 

document.querySelector('[data-resto-id="<%= j @resto.id%>"]').remove()
document.querySelector('[data-genre-id="<%= j @genre_after.id %>"]')
    .children[1]
    .insertAdjacentHTML(
        'beforeend',
        `<span  draggable="true" class="resto_tag <%= j @resto.color%>" data-resto-id="<%= j @resto.id%>"> <%= j @resto.name %> </span>`
    )
//document.querySelector('#form_resto_genre').reset()
